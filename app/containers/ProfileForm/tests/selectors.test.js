// import { fromJS } from 'immutable';
// import { makeSelectProfileFormDomain } from '../selectors';

// const selector = makeSelectProfileFormDomain();

describe('makeSelectProfileFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
