// import { fromJS } from 'immutable';
// import { makeSelectCreateSignalDomain } from '../selectors';

// const selector = makeSelectCreateSignalDomain();

describe('makeSelectCreateSignalDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
