// import { fromJS } from 'immutable';
// import { makeSelectSignalDomain } from '../selectors';

// const selector = makeSelectSignalDomain();

describe('makeSelectSignalDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
